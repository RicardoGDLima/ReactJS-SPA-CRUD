import styled from 'styled-components'

export const Container = styled.header`   

    background: #FFF;
    padding: 0px 15px;
    overflow: hidden;
    white-space: nowrap;
    box-shadow: var(--shadow) 
`