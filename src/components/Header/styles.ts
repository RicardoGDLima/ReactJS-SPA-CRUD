import styled from 'styled-components'

export const Container = styled.header`   

    background: #FFF;
    padding: 0 15px;
    overflow: hidden;
    white-space: nowrap;
    box-shadow: var(--shadow) 
`